import React from "react";
import { useAuth } from "../../Hooks/useAuth";

const ChatMessage = ({ message, senderUserName }) => {
  const { user } = useAuth();
  return <div>
    
  </div>;
};

export default ChatMessage;
